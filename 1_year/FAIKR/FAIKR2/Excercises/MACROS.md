# Built-in predicates in prolog

## Lists

1) ```
   is_list(L)
   ```
2) ```
   append(L1, L2, L1_L2)
   ```
3) ```
   length(L, len_of_L)
   ```
4) ```
   reverse(L, reversed_L)
   ```
5) ```
   member(E, L)
   ```
6) ```
   sort(L, L_sorted)
   ```
7) 
8)
